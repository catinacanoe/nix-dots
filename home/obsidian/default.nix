{ config, pkgs, ...}:
{
    # this option doesn't exist in 24.11 but does in 25.05 (configure once we update)

    # programs.obsidian = {
    #     enable = true;

    #     vaults = {
    #         main = {
    #             enable = true;
    #             target = "dox/obsidian/main/";
    #         };
    #     };

    #     defaultSettings = {

    #     };
    # };
}
